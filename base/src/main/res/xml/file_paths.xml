<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Cache directory -->
    <cache-path
        name="cache"
        path="/" />

    <!-- Files directory -->
    <files-path
        name="files"
        path="/" />

    <!-- External cache directory -->
    <external-cache-path
        name="external_files"
        path="/" />

    <!-- Internal files directory -->
    <files-path
        name="internal_files"
        path="." />
</paths>